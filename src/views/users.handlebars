<div>
    {{#each}}
    <div>
        <p>Mp,nre: {{this.first_name}} &nbsp; Apellido: {{this.last_name}}</p>
    </div>
    {{/each}}

</div>
<br/>
<div>
    {{#if hasPrevPage}}
        <a href="{{this.prevLink}}"><< Anterior</a>
    {{/if}}
    &nbsp;

    {{this .page}}
    &nbsp;

    {{#if hasNextPage}}
        <a href="{{this.nextLink}}">Siguiente >></a>
    {{/if}}
{{else}}
<h1>No hay usuarios para mostrar</h1>
{{/if}}